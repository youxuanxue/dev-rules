{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/youxuanxue/dev-rules/schemas/review.schema.json",
  "title": "Code Review Report",
  "description": "Schema for /user:review JSON output. Default reports stay concise; high-risk reviews add conformance evidence.",
  "type": "object",
  "required": ["review_date", "scope", "risk_level", "review_mode", "decision", "summary", "findings"],
  "additionalProperties": true,
  "properties": {
    "review_date": {
      "type": "string",
      "format": "date",
      "description": "ISO date when this review was generated"
    },
    "scope": {
      "type": "string",
      "description": "git range reviewed, e.g. 'HEAD~5..HEAD' or 'last 24h'"
    },
    "risk_level": {
      "type": "string",
      "enum": ["low", "normal", "high"],
      "description": "Risk classification of the reviewed change"
    },
    "review_mode": {
      "type": "string",
      "enum": ["concise", "full_conformance"],
      "description": "Concise is the default; full_conformance is used for high-risk changes"
    },
    "decision": {
      "type": "string",
      "enum": ["merge-ready", "needs-fix", "needs-design-review"],
      "description": "Top-level reviewer decision"
    },
    "approved_artifacts_referenced": {
      "type": "array",
      "items": { "type": "string" },
      "description": "List of docs/approved/ files actually consulted as conformance baseline"
    },
    "validation_gaps": {
      "type": "array",
      "items": { "type": "string" },
      "description": "Notable tests or checks that were not run"
    },
    "summary": {
      "type": "object",
      "required": ["critical", "high", "medium", "low"],
      "properties": {
        "critical": { "type": "integer", "minimum": 0 },
        "high":     { "type": "integer", "minimum": 0 },
        "medium":   { "type": "integer", "minimum": 0 },
        "low":      { "type": "integer", "minimum": 0 }
      }
    },
    "conformance_summary": {
      "type": "object",
      "required": ["total_findings", "by_artifact", "artifacts_checked"],
      "properties": {
        "total_findings": { "type": "integer", "minimum": 0 },
        "artifacts_checked": {
          "type": "array",
          "items": { "type": "string" }
        },
        "by_artifact": {
          "type": "object",
          "additionalProperties": { "type": "integer", "minimum": 0 }
        }
      }
    },
    "findings": {
      "type": "array",
      "items": {
        "type": "object",
        "required": ["severity", "category", "automatable", "file", "description", "human_verdict"],
        "properties": {
          "severity": {
            "type": "string",
            "enum": ["critical", "high", "medium", "low"]
          },
          "category": {
            "type": "string",
            "enum": ["code-quality", "security", "test-coverage", "architecture", "contract", "input-safety", "design-quality", "conformance", "scope-creep"]
          },
          "automatable": { "type": "boolean" },
          "file": { "type": "string", "minLength": 1 },
          "line": { "type": ["integer", "null"], "minimum": 0 },
          "description": { "type": "string", "minLength": 1 },
          "reference": {
            "type": ["string", "null"],
            "description": "Required when category=conformance: points to approved-artifact path#section"
          },
          "suggested_fix": { "type": ["string", "null"] },
          "human_verdict": {
            "type": "object",
            "description": "Empty object when generated; reviewers fill in during calibration",
            "additionalProperties": false,
            "properties": {
              "accurate":         { "type": "boolean" },
              "severity_correct": { "type": "boolean" },
              "autofix_safe":     { "type": "boolean" },
              "notes":            { "type": "string" }
            }
          }
        },
        "allOf": [
          {
            "if":   { "properties": { "category": { "const": "conformance" } } },
            "then": { "required": ["reference"], "properties": { "reference": { "type": "string", "minLength": 1 } } }
          }
        ]
      }
    },
    "circuit_breaker": {
      "type": "object",
      "required": ["max_autofixes", "budget_usd_used", "tripped"],
      "properties": {
        "max_autofixes":   { "type": "integer", "minimum": 0 },
        "budget_usd_used": { "type": "number",  "minimum": 0 },
        "tripped":         { "type": "boolean" },
        "trip_reason":     { "type": ["string", "null"] }
      }
    }
  },
  "allOf": [
    {
      "if": {
        "properties": {
          "review_mode": { "const": "full_conformance" }
        }
      },
      "then": {
        "required": ["approved_artifacts_referenced", "conformance_summary"],
        "properties": {
          "approved_artifacts_referenced": {
            "minItems": 1
          }
        }
      }
    }
  ]
}
