---
description: IT 产品研发工作流规则，含原型设计审批环节，适用于所有产品开发任务
alwaysApply: true
---

# 产品研发工作流

## 研发阶段流程（必须按顺序通过）

```
需求分析 → 原型设计 → [人工审批] → 功能实现 → 测试验证 → [人工审批] → 合并上线
```

每个阶段都有明确的产出物和审批门禁。Agent 不得跳过任何阶段。

### 阶段 1：需求分析

**产出**：需求拆解文档（`docs/task-breakdown-*.md`）
**动作**：
1. 理解业务目标和用户价值
2. 拆解为可执行子任务（使用 `/user:decompose` 命令）
3. 标注子任务间的依赖与并行关系
**审批**：拆解结果需人工确认后进入下一阶段

### 阶段 2：原型设计与实现

**产出**：可运行的原型系统 + 设计文档（直接写入 `docs/approved/`）
**动作**：
1. 将设计文档直接写入 `docs/approved/`（实体化命名，如 `design-approval-engine.md`），包含：
   - YAML frontmatter 元数据头（`approved_by: pending`，待人审批时更新）
   - 数据模型设计（ER 图或表结构）
   - API 接口设计（路由、请求/响应格式）
   - 核心流程的状态机或时序图
   - 技术选型与理由
2. 实现最小可运行原型（Prototype），仅覆盖核心路径
3. 原型必须可启动、可演示核心流程，但无需完善边界处理
4. 编写原型验证脚本或手动验证步骤
**提交**：创建 PR，标题前缀 `prototype:`，包含 `docs/approved/` 下的设计文档和演示说明

### ▶ 审批门禁：原型确认

**Agent 在此暂停，等待人工审批。**

审批内容：
- [ ] 数据模型是否合理
- [ ] API 设计是否符合现有规范
- [ ] 技术选型是否恰当
- [ ] 原型是否可演示核心流程
- [ ] 是否需要调整设计方向

审批方式：人在 PR 中直接编辑/修正 `docs/approved/` 下的设计文档，merge = 审批。

审批结果：
- **通过（merge）** → `docs/approved/` 中的文件成为符合性基线，进入功能实现阶段
- **需修改** → 人在 PR 中提出修改意见，Agent 修改后重新请求 Review
- **否决** → 终止当前方案，回到需求分析重新规划

### 阶段 3：功能实现

**产出**：生产级代码 + 测试 + 文档
**动作**：
1. 基于已审批的原型，补全所有边界处理和错误处理
2. 编写完整的测试套件（遵循 test-philosophy.mdc）
3. 更新 API 契约文档（遵循 agent-contract-enforcement.mdc）
4. 每个功能点分批提交，每个提交可独立 Review
**提交**：创建 PR，标题前缀 `feat:`

### 阶段 4：测试验证

**产出**：测试报告 + 验收确认
**动作**：
1. 运行完整测试套件，确保不破坏已有功能
2. 验证所有验收标准（AC）已覆盖
3. 检查 Story ↔ Test 对齐（无漂移）
4. 生成变更摘要：改了什么、为什么改、如何验证
**提交**：更新 PR，附带测试报告和验收矩阵

### ▶ 审批门禁：合并确认

**Agent 在此暂停，等待人工审批。**

审批内容：
- [ ] 代码质量是否达标
- [ ] 测试覆盖是否充分
- [ ] 是否影响其他模块
- [ ] 是否可以合并到主分支

## 分支与提交

- 原型分支命名：`prototype/[issue-id]-[描述]`
- 功能分支命名：`feature/[issue-id]-[描述]`
- 修复分支命名：`fix/[issue-id]-[描述]`
- 提交消息聚焦"为什么"而非"改了什么"
- 每个 PR 只解决一个问题，不混合无关变更

## 代码质量门禁

- 新增功能：至少 1 个正向测试 + 1 个负向测试
- Bug 修复：必须包含复现测试
- 重构：已有测试必须原样通过
- 禁止存在性测试（仅断言文件存在/非空），必须是行为断言

## 长时运行任务专用

当作为 Background Agent 或 Long-running Agent 执行时：
- 每完成一个可验证的里程碑，立即提交并推送
- 如果连续 3 次尝试同一问题未成功，暂停并记录分析，等待人工介入
- 产出 PR 时附带：变更说明、测试结果、待决策问题清单
- 原型阶段完成后必须暂停等待审批，不得自行进入功能实现阶段

## 完成自检（提交前必做）

每次任务完成、准备提交或汇报前，Agent 必须对本次所有变更执行一致性自检：

1. **跨文件一致性**：同一概念（路径、命名、日期、术语）在多个文件中的描述是否一致
2. **新旧对齐**：变更是否与 `docs/approved/`、`CLAUDE.md`、已有 Rules 中的约定冲突
3. **引用有效性**：文件路径、命令名、章节引用是否指向真实存在的目标
4. **冗余消除**：同一信息是否在多处重复表述（用不同措辞描述同一件事会导致漂移）
5. **示例准确性**：代码示例、JSON 样例、目录结构图是否反映当前实际状态

发现问题直接修复，修复后再提交。如果不确定是否应该改，在 PR 说明中标注待确认项。

## 云端 Agent 调用 Claude Code CLI

当需要在云端 VM 中执行 `claude -p` 命令（审查、拆解、校准等）时：

1. 先运行 `bash scripts/setup-claude-code.sh` 检查并安装 Claude Code CLI
2. API Key 通过 Cursor Dashboard → Cloud Agents → Secrets 配置（`ANTHROPIC_API_KEY`）
3. 安装完成后即可执行 `claude -p "..." --max-budget-usd N`
