---
description: dev-rules submodule 约定：规则的单一事实来源管理规范
alwaysApply: true
---

# dev-rules 约定

## 项目结构

每个 IT 产品研发项目必须包含 `dev-rules` 作为 git submodule：

```
项目根目录/
├── dev-rules/              ← git submodule → git@github.com:youxuanxue/dev-rules.git
│   ├── rules/*.mdc         ← 规则原件（唯一编辑入口）
│   ├── commands/*.md        ← Claude Code 自定义命令
│   └── sync.sh              ← 分发脚本
└── .cursor/rules/*.mdc     ← sync 产物（禁止直接编辑）
```

## 新项目接入

```bash
cd 项目根目录
git submodule add git@github.com:youxuanxue/dev-rules.git dev-rules
dev-rules/sync.sh --local
git add .cursor/rules/ .gitmodules dev-rules
git commit -m "chore: add dev-rules submodule and sync rules"
```

## 编辑规则

涉及 `dev-rules/` 的 Git 提交**必须**遵守「先子模块、后父仓库」顺序（详见下一节）。其余步骤如下：

1. 在任意项目内编辑 `dev-rules/rules/*.mdc`
2. 运行 `dev-rules/sync.sh --local` 同步到当前项目的 `.cursor/rules/`
3. 在子模块仓库内提交并推送（先于父仓库）：
   ```bash
   cd dev-rules && git add -A && git commit -m "update rules" && git push && cd ..
   ```
4. 回到项目根目录，再提交父仓库的子模块指针与 `.cursor/rules/`，并推送：
   ```bash
   git add dev-rules .cursor/rules/ && git commit -m "chore: sync dev-rules" && git push
   ```

## Git 提交顺序（强制）

凡变更 `dev-rules/`（含 `rules/`、`commands/`、`sync.sh` 等子模块内任意文件），**必须先**在子模块仓库完成提交（并 `git push` 到远端），**再**在父项目提交「子模块指针 + `.cursor/rules/` 同步产物」。

**正确顺序**：

1. 子模块：`cd dev-rules` → `git add -A` → `git commit` → `git push`
2. 父仓库：`cd ..` → `git add dev-rules .cursor/rules/` → `git commit` → `git push`

**原因**：父仓库中的 `dev-rules` 记录的是子模块某一提交的 SHA；若先提交父仓库而子模块尚无对应 commit，协作者与 CI 会出现无法解析的悬空指针或「有 sync 产物但规则仓库无历史」的不一致。

**禁止**：在子模块未提交（或未推送、若团队以远端为准）的情况下，单独在父仓库 `git commit` 更新 `dev-rules` 子模块引用或仅提交 `.cursor/rules/` 而不同步子模块上游。

## 强约束门禁（机械检查，禁止仅"靠自觉"）

软规则配套有可执行脚本，提交/CI 必须运行：

| 软规则 | 硬约束脚本 | 触发时机 |
|--------|----------|---------|
| 「`.cursor/rules/` 与 submodule 不漂移」 | `dev-rules/sync.sh --check` | pre-commit / CI |
| 「dev-rules 仓库自身完整性（README、frontmatter、哲学映射）」 | `dev-rules/verify-rules.sh` | dev-rules 子模块提交前 |
| 「先子模块后父仓库」（submodule SHA 必须在 dev-rules 中真实存在） | `scripts/preflight.sh` 检查段 2 | pre-commit / CI |
| 「review JSON 输出格式合规」 | `dev-rules/schemas/review.schema.json` | `/user:calibrate` 入口处 |
| 「契约文档不漂移」 | `python scripts/export_agent_contract.py --check` | `scripts/preflight.sh` 检查段 4 / CI |
| 「分支命名 `prototype/|feature/|fix/`」 | `scripts/preflight.sh` 检查段 1 | pre-commit |
| 「`docs/approved/` 仅在 GATE PR 中改动」 | `scripts/preflight.sh` 检查段 6 | PR 检查 |

新项目接入时，将 `dev-rules/templates/preflight.sh` 复制到 `scripts/preflight.sh` 并按需启用各检查段。CI workflow 至少应运行 `dev-rules/sync.sh --check` 与 `scripts/preflight.sh`。

## 禁止事项

- 禁止直接编辑 `.cursor/rules/` 中的文件（它们是 sync 产物）
- 禁止绕过 submodule 手动复制规则文件
- 禁止在 `.cursor/rules/` 中创建不在 `dev-rules/rules/` 中的规则文件
- 禁止在 `dev-rules/sync.sh --check` 失败的情况下提交父仓库
- 禁止在 `dev-rules/verify-rules.sh` 失败的情况下提交 dev-rules 子模块

## Agent 行为约束

- 如果发现项目缺少 `dev-rules/` submodule，提醒用户按上述步骤接入
- 如果发现 `.cursor/rules/` 与 `dev-rules/rules/` 内容不一致，运行 `dev-rules/sync.sh --local` 修复（提交前必须再跑 `--check` 确认归零）
- 修改规则时，编辑 `dev-rules/rules/` 中的文件 → `sync.sh --local` → `verify-rules.sh` → 提交
- 涉及 `dev-rules/` 的提交与推送时，**必须**按「Git 提交顺序（强制）」先子模块、后父仓库；不得颠倒顺序或只提交父仓库而不同步子模块上游
